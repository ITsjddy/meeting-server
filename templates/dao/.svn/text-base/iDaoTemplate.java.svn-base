#set ($domain = $!domainName.substring(0,1).toLowerCase()+$!domainName.substring(1))package $!{packageName}.modules.$!{floder}.dao;

import com.smartdot.meeting.server.common.dao.GenericDao;
import com.smartdot.meeting.server.common.entity.$!{domainName};
#set ($domain = $!domainName.substring(0,1).toLowerCase()+$!domainName.substring(1))
public interface I$!{domainName}Dao extends GenericDao<$!{domainName}> {
	public final static String DAO_NAME="$!{domain}Dao";
	
}
